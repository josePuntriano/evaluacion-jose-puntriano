serenity {
    project.name = "Evaluacion Jose Puntriano"
    take.screenshots = FOR_FAILURES
}

webdriver {
    driver = chrome
    autodownload = true
}

headless.mode = false

cucumber {
    tags = "@regression"
}